using UnityEngine;
using System.Collections;

public class key1Setup : MonoBehaviour {

	// Use this for initialization
	void Start () {
        LeapStatic.resetConstructionObject("key1");

    }

    // Update is called once per frame
    void Update () {
	
	}
}
